<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.lingkarinovasimuda.githubuserapp">

    <!-- Internet Permission -->
    <uses-permission android:name="android.permission.INTERNET" />

    <permission android:name="com.lingkarinovasimuda.githubuserapp.READ_DATABASE" android:protectionLevel="normal"/>
    <permission android:name="com.lingkarinovasimuda.githubuserapp.WRITE_DATABASE" android:protectionLevel="normal"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <provider
            android:name=".provider.FavoriteUserProvider"
            android:authorities="com.lingkarinovasimuda.githubuserapp"
            android:enabled="true"
            android:exported="true"
            android:readPermission="com.lingkarinovasimuda.githubuserapp.READ_DATABASE"
            android:writePermission="com.lingkarinovasimuda.githubuserapp.WRITE_DATABASE"/>

        <receiver
            android:name=".services.AlarmReceiver"
            android:enabled="true"
            android:exported="true" />

        <activity android:name=".activity.SettingActivity" />
        <activity
            android:name=".activity.FavoriteActivity"
            android:label="@string/my_favorite_user" />
        <activity
            android:name=".activity.FollowersFollowingActivity"
            android:label="@string/title_activity_followers_following"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".activity.DetailActivity" />
        <activity
            android:name=".activity.MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>